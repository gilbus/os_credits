---
repos:
  - repo: local
    hooks:
      - id: pytest
        name: Run tests
        entry: poetry run python -m pytest -v --color=yes
        pass_filenames: false
        language: system
        types: [python]
      - id: black
        name: Blacken code
        entry: poetry run black os_credits
        pass_filenames: false
        language: system
        types: [python]
